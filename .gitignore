# Kilo Code state / registry (generated)
.kilo/
.kilocode/

# pipa_harness repo gitignore
graphify-out/
*.local.*
state/*.local.md
state/scratch/
state/*.pid
state/*.log
.env
.env.*.local

# extension machinery (local merges stay local)
.pipa-extensions/

# OS
.DS_Store

# Obsidian workspace state (committed app config stays)
.obsidian/workspace*.json
