# Example rule — rename to <domain>.md

Project rules load automatically from `.pipa/rules/*.md` (alongside the
global install's rules). Keep them short and imperative.

- [HARD] Rules that must never be violated in this repo.
- [SOFT] Preferences the agent should follow when possible.
