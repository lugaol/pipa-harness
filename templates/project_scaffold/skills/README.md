# Project skills

Drop `skills/<name>/SKILL.md` here for skills used ONLY by this project.
A project skill with the same name as a global skill takes precedence.

Global/shared skills live in the pipa install (`<install>/skills/`) — do
not copy them here.
