# Example rule — rename this file to <domain>.md and scope it

Rules attach automatically by path glob via `instructions` in
`.opencode/opencode.jsonc` (e.g. `.harness_extension/rules/*.md`).
Keep rules short and imperative.

- [HARD] Rules that must never be violated.
- [SOFT] Preferences the agent should follow when possible.
